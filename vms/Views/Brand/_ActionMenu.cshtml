@model vms.entity.models.Brand 
@{
	var session = Context.Session.GetComplexData<VmUserSession>(ControllerStaticData.SESSION);
}
<grid-freeze-menu>
	@if (UserAuthorization.Check(FeatureList.AdministrationBrandCanEdit, session.Rights))
	{
		<li>
			<a class="dropdown-item" asp-action="Edit" asp-route-id="@Model.EncryptedId"><i class="bi bi-pencil-square"></i> Edit</a>
		</li>
	}
</grid-freeze-menu>