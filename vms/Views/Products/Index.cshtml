@model vms.entity.viewModels.vmProductsIndex
@{
	ViewData["Title"] = "Products/Services";
	var session = Context.Session.GetComplexData<VmUserSession>(ControllerStaticData.SESSION);
}

@section contentheader
{
	<div class="p-2 page-header d-flex">
		<div class="flex-grow-1">
			@ViewData["Title"]
		</div>
		<div class="d-flex justify-content-start">

			@if (UserAuthorization.Check(FeatureList.PRODUCT_PRODUCT_LIST_CAN_ADD, session.Rights))
			{
				<div class="me-1">
					<a asp-controller="Products" asp-action="Create" class="btn btn-primary btn-sm"><i class="bi bi-plus-lg"></i> Product/Service</a>
				</div>
				@* <div class="me-1">
					|
				</div>
				<div class="me-1">
					<a asp-controller="Products" asp-action="CreateProductByType" asp-route-typeName="@EnumProductType.ServiceMeasurable" class="btn btn-success btn-sm"><i class="bi bi-plus-lg"></i> Measurable Service</a>
				</div>
				<div class="me-1">
					<a asp-controller="Products" asp-action="CreateProductByType" asp-route-typeName="@EnumProductType.ServiceUnmeasurable" class="btn btn-info btn-sm"><i class="bi bi-plus-lg"></i> Un-measurable Service</a>
				</div>
				<div class="me-1">
					|
				</div>
				<div class="me-1">
					<a asp-controller="Products" asp-action="CreateProductByType" asp-route-typeName="@EnumProductType.ConsumableProduct" class="btn btn-outline-success btn-sm"><i class="bi bi-plus-lg"></i> Consumable Product</a>
				</div>
				<div class="me-1">
					<a asp-controller="Products" asp-action="CreateProductByType" asp-route-typeName="@EnumProductType.ConsumableServiceUnMeasurable" class="btn btn-outline-dark btn-sm"><i class="bi bi-plus-lg"></i> Consumable Un-measurable Service</a>
				</div> *@
			}
		</div>
	</div>
}


<scrollable-grid-container>
	<partial name="_Index" model="Model.ProductList"/>
</scrollable-grid-container>